{
  "name": "chat-client",
  "version": "0.0.1",
  "scripts": {
    "start": "live-server --port=4200",
    "prettier": "prettier --config ./.prettierrc --list-different \"**/*{.html,.js,.json,.css}\" --write"
  },
  "dependencies": {
    "bits-utils": "^1.0.5",
    "live-server": "^1.2.1"
  },
  "devDependencies": {
    "prettier": "^2.0.2",
  }
}
